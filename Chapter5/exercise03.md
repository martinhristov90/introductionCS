### Variables full and empty refer to Boolean values. 
### Write an expression that produces True if and only if at most one of the variables is True.

```python

(full or empty) and not (full and empty)

```